import json
import sys

# 模拟前端发送的请求
frontend_request = {
    "model": "gemini-3-flash",
    "temperature": 0.35,
    "response_format": {"type": "json_object"},
    "messages": [
        {
            "role": "system",
            "content": """你是云智道AI工作台内部使用的短视频文案生成系统。
你的核心任务：生成能拿到真实流量、停留、评论、精准关注的短视频文案。

=== 皮（开头钩子）的写法标准 ===
皮是第一注意力武器，不是一句观点或总结，而是一把抓人的钩子。

【仿写爆款任务的钩子写法 - 重要】
当任务类型是「仿写爆款」时，钩子必须基于参考文案的核心内容来写，严禁使用业务关键词。
正确做法：
- 参考文案说「再过五年，买不起的不再是房子，而是这三样东西」
- 钩子应该改写成「2025年开始，老板们最焦虑的不再是没订单，而是这三样东西你花多少钱都买不到」
- 保留核心元素：时间（五年→2025年）、数字（三样东西）、结论（买不起）
错误做法：
- 钩子写成「数字资产这件事出来，很多普通人后背发凉了」（自己编的，跟原文无关）
- 钩子写成「刘强东沉默了，马斯克也急了」（编造原文没有的人物）

=== 你服务的业务方向 ===
我是谁/做什么：我是做AI获客和内容增长的
注意：核心关键词仅用于业务植入（肉）和收口，严禁在钩子（皮）中使用。"""
        },
        {
            "role": "user",
            "content": """入口方式：仿写爆款
挂业务方式：不挂业务
收口方式：纯评论流量
参考文案：再过五年，老百姓买不起的不再是房子，而是这三样东西，尤其是第三个。
你的要求：无
任务理解：这是仿写爆款任务。
优先拆结构、改表达、保留停留感，不要原样复述参考文案。

请生成 6 条强力开头钩子（皮）。

【核心内容】
再过五年，老百姓买不起的不再是房子，而是这三样东西，尤其是第三个。

【仿写爆款 - 钩子生成要求】
1. 严禁使用业务关键词（如"数字资产"、"AI获客"、"内容增长"等），钩子必须100%基于参考文案的核心内容。
2. 提取原文的核心元素：
   - 时间元素（如"再过五年"、"2025年"、"未来三年"）
   - 数字元素（如"三样东西"、"这一件事"、"两个选择"）
   - 结论元素（如"买不起"、"来不及"、"没机会"）
3. 用这些核心元素改写成更炸的钩子，但不能改变原文的核心命题。
4. 6条钩子要在表达方式上有差异，但核心命题必须一致。

【严禁事项】
- 严禁在钩子中使用业务关键词（数字资产、AI获客、内容增长、私域沉淀等）。
- 严禁编造原文中没有的人物、事件、热点（如马斯克、刘强东、李亚鹏等）。
- 严禁改变原文的核心命题，必须保留原文的核心元素。"""
        }
    ]
}

# 模拟后端处理
BACKEND_PROMPT_PREFIX = "\n".join([
    "你在为云智道AI工作台服务。",
    "皮不是一句普通观点，而是第一注意力武器。",
    "处理皮时，必须先判断核心命题、目标人群、核心冲突、具体后果，再写第一句。",
    "皮可以优先使用 6 类主触发器：强判断、强命令、极端后果、热点爆料、反常识提问、信息差利益。",
    "每条皮至少叠加 1 个强化器：名人、平台、时间点、数字、情绪反应、人群点名、具体损失、强反差、时代终结、悬念揭晓词。",
    "第一句必须抓人，要像判决、爆料、命令、预警，而不是解释或总结。",
    "只输出强力版，不要温和版，不要折中版，不要安全版。",
    "不要只写抽象大词，必须尽量落到具体对象、具体冲突、具体损失。",
    "禁止使用这类软起手：很多人以为、我把话说重一点、真正可怕的不是、旧逻辑已经死了。",
    "生成正文时，也禁止高频使用这类空话：说到底、真正拉开差距的、很多老板其实、你可以现在还没全做。",
])

backend_modified_request = {
    **frontend_request,
    "messages": [
        {"role": "system", "content": BACKEND_PROMPT_PREFIX},
        *frontend_request["messages"]
    ]
}

print("=" * 80)
print("🔍 问题分析：后端会在前端的 system prompt 前面插入自己的 prompt")
print("=" * 80)
print()

print("📤 前端发送的 system prompt（前 500 字符）：")
print(frontend_request["messages"][0]["content"][:500])
print()

print("🔧 后端实际发送给 API 的 messages 结构：")
print(f"  - messages[0] (后端插入): {len(BACKEND_PROMPT_PREFIX)} 字符")
print(f"  - messages[1] (前端 system): {len(frontend_request['messages'][0]['content'])} 字符")
print(f"  - messages[2] (前端 user): {len(frontend_request['messages'][1]['content'])} 字符")
print()

print("⚠️ 问题：")
print("  1. 后端的 BACKEND_PROMPT_PREFIX 太简化，没有「严禁使用业务关键词」的约束")
print("  2. 后端的 prompt 没有「严禁编造人物」的约束")
print("  3. 后端的 prompt 没有「必须基于参考文案」的约束")
print("  4. 多个 system 消息可能导致模型混淆，不知道该听谁的")
print()

print("💡 解决方案：")
print("  方案1：删除后端的 BACKEND_PROMPT_PREFIX，让前端完全控制 prompt")
print("  方案2：后端只在前端没有 system prompt 时才添加")
print("  方案3：后端的 BACKEND_PROMPT_PREFIX 改为追加到前端 system prompt 后面")
print()

print("=" * 80)
print("📝 建议：使用方案1，删除后端的 BACKEND_PROMPT_PREFIX")
print("=" * 80)
